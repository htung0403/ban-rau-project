
### Task 7 & 8 Completed
- Updated `CustomerDetailPage.tsx` to dynamically add the "Đơn hàng" tab for loyal customers.
- Implemented inline unit_price editing, order selection checkboxes, and saving logic calling `useUpdateDeliveryOrderPrices`.
- Implemented print functionality using `window.print()` and added `@media print` CSS rules in `client/src/index.css`.
- Leveraged `data-print-area`, `data-print-row`, and `data-print-hide` to control visibility in print dialog without rendering a custom modal.
- Handled all TypeScript typings properly and ensured `tsc --noEmit` passes.

